### 1. 홈화면
<API Endpoint>
GET /users/{user-id}/home

<Request Body>
없음

<Request Header>
Authorization : accessToken (String)

<Query String>
?name=umc

### 2. 마이 페이지 리뷰 작성
<API Endpoint>
POST /users/{user-id}/reviews/{review-id}

<Request Body>
```json
{
	"detail" : "음 너무 맛있어요"
}
```
<Request Header>
Authorization : accessToken (String)

<Query String>
없음

### 3. 미션 목록 조회(진행중, 진행 완료)
<API Endpoint>
GET /users/{user-id}/missions

<Request Body>
없음

<Request Header>
Authorization : accessToken (String)

<Query String>
없음

### 4. 미션 성공 누르기
<API Endpoint>
POST /users/{user-id}/missions/{mission-id}

<Request Body>

```json
{
	"isSuccess" : "true"
}
```

<Request Header>
Authorization : accessToken (String)

<Query String>
없음

### 5. 회원 가입 하기
<API Endpoint>
POST /users

<Request Body>
```json
{
	"이름" : "UMC",
	"성별" : "남",
	"생년월일" : "1999/01/15",
	"주소" : "알고싶으면 연락하세요",
	"선호하는 음식 종류" : "치킨"
}
```

<Request Header>
Authorization : accessToken (String)

<Query String>
없음
